#!/bin/sh
#
# Use this to compile CoffeeScript into JS
coffee --map --compile --output build/ src/counter.coffee
coffee --bare --compile --output build/spec spec
echo CoffeeScript compiled successfully